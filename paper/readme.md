# Paper for EnCoDaPy

- The [paper](./paper.md) describes the framework.
- Use this Docker command from the main path to build a PDF:

```shell
docker run --rm --volume $PWD/paper:/data --env JOURNAL=joss openjournals/inara
```
